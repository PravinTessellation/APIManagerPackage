import XCTest
@testable import APIManagerPackage

final class APIManagerPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(APIManagerPackage().text, "Hello, World!")
    }
}
